To install Bombo, run these commands in a terminal program, in the same directory as the archive file:

    tar -xvzf bombo-installer.tar.gz
    cd bombo_installer
    ./install.sh

To run the executable:

    ./Bombo.exe

Simply move this executable to anywhere in the directory you wish to work in.